-r requirements.txt

# These dependencies are installed in lambda layer AWSLambdaPowertoolsPythonV3-python312,
# only need to install them in local development environment
aws-lambda-powertools==3.4.1
boto3
botocore
pydantic==2.12.2
python-dotenv==1.0.1

# These dependencies are only needed for local testing
pre-commit
pylint
pytest
coverage
python-lambda-local
moto[dynamodb]
